trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

variables:
- group: azurecli-shared-resources

steps:
- task: AzureCLI@2
  inputs:
    azureSubscription: 'Development'
    scriptType: 'bash'
    scriptLocation: 'scriptPath'
    scriptPath: './initial_config.sh'
  displayName: 'Create shared resources'
